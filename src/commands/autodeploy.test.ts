import { Context } from "../models/context"
import { autoDeploy } from "./autodeploy"

describe("auto-deploy command", () => { 

    it("real", () => {
        const c = new Context()

        // autoDeploy(c)
    })
})